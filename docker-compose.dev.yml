version: "3.9"
services:
  cloudclip:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
      PORT: 5000
      SESSION_SECRET: change-me-in-production
    volumes:
      - ./clipboard.db:/app/clipboard.db
